name: Snyk SAST Scan

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  snyk-sast:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Snyk CLI
        uses: snyk/actions/setup@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # 🔎 Run Snyk Code (SAST)
      - name: Run Snyk Code (SAST)
        id: snyk-sast
        run: snyk code test --sarif-file-output=snyk-sast.sarif
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # 📤 Upload SAST results only if file exists
      - name: Upload SAST results
        if: ${{ steps.snyk-sast.outcome == 'success' && always() && (filesExist('snyk-sast.sarif')) }}
        run: |
          if [ -f snyk-sast.sarif ]; then
            gh api repos/${{ github.repository }}/code-scanning/sarifs \
              -f sarif=@snyk-sast.sarif
          else
            echo "No SAST SARIF to upload"
          fi
